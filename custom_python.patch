diff --git a/src/python/CMakeLists.txt b/src/python/CMakeLists.txt
index 81a4a89..8d28b2f 100644
--- a/src/python/CMakeLists.txt
+++ b/src/python/CMakeLists.txt
@@ -9,6 +9,7 @@ else ()
 endif ()
 
 if (NOT BOOST_CUSTOM AND NOT BUILD_PY3)
+    message(STATUS "FIND PYTHON")
     #Unset those, otherwise find_package(PythonLibs) will pick up old stuff
     #if it has been run before
     unset(Python_ADDITIONAL_VERSIONS)
@@ -18,6 +19,10 @@ if (NOT BOOST_CUSTOM AND NOT BUILD_PY3)
     unset(PYTHON_INCLUDE_DIR CACHE)
     unset(PYTHON_INCLUDE_PATH)
     unset(PYTHON_INCLUDE_PATH CACHE)
+    if(PYTHON_HOME)
+      set(PYTHON_LIBRARY ${PYTHON_HOME}/lib/)
+      set(PYTHON_INCLUDE_DIR ${PYTHON_HOME}/include/python2.7)
+    endif()
     find_package (PythonLibs ${PYTHON_VERSION} REQUIRED)
     find_package (Boost 1.42 REQUIRED COMPONENTS python)
 elseif (BOOST_CUSTOM AND NOT BUILD_PY3)
@@ -122,7 +127,7 @@ if (BOOST_CUSTOM OR Boost_FOUND AND PYTHONLIBS_FOUND)
                                DEBUG_POSTFIX "_d"
                                SUFFIX ".pyd")
     endif()
-    
+
     if (BUILD_PY3)
         install (TARGETS ${target_name}
                  RUNTIME DESTINATION ${PYLIB3_INSTALL_DIR} COMPONENT user
